# Similarity threshold for semantic cache (0.0–1.0)
# Lower = more cache hits (but risk of false matches)
# Higher = fewer cache hits (more conservative)
# Recommended: 0.85–0.92
SIMILARITY_THRESHOLD=0.88

# Number of clusters (must match what 02_fuzzy_cluster.py used)
N_CLUSTERS=15
